apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: kubeguardian-demo
  labels:
    app: backend
    app.kubernetes.io/part-of: kubeguardian-demo
data:
  PORT: "5000"
  NODE_ENV: production
